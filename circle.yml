dependencies:
  post:
    - bower update

compile:
  override:
    - gulp compile

test:
  override:
    - echo test

deployment:
  production:
    branch: master
    commands:
      - rsync -avz --delete --exclude ".git*" --exclude "node_modules" --exclude "v2" --exclude "fonts" . $UIKIT_SERVER
