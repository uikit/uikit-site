dependencies:
  pre:
    - bower update

compile:
  override:
    - gulp compile

test:
  override:
    - echo test

deployment:
  production:
    branch: master
    commands:
      - rsync -avz --delete --exclude ".git*" --exclude "node_modules" --exclude "docs/fonts" --exclude "v2" . $UIKIT_SERVER
